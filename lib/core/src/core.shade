module core (
    module core,
    module c,
    libc,
) =

import prim as c
import intrinsics (($), unsafe, crash, partial) as c
import core/ops as c
import core/io (print, println, eprint, eprintln) as c
