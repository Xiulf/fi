module Core (
    module Core,
    module C,
    Libc,
) =

import Prim as C
import Intrinsics (($), unsafe, crash, partial) as C
import Core.Ops as C
import Core.Error (unwrap, unwrap_or, ret, (!), (?)) as C
import Core.Data.Option (Option, Some, None) as C
import Core.Data.Result (Result, Ok, Error) as C
import Core.Data.List (List, Cons, Nil, (:)) as C
