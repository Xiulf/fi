module Data:Char (
    ; encode_utf8, len_utf8
) =

import Prim (Uint32, Char)

; fun encode_utf8 code buf = do
    ; let len = len_utf8 code
; 
; fun len_utf8 code =
;     if code < MAX_ONE_B
;     then 1
;     else if code < MAX_TWO_B
;     then 2
;     else if code < MAX_THREE_B
;     then 3
;     else 4
; 
; const MAX_ONE_B   = 0 :: Uint32
; const MAX_TWO_B   = 0 :: Uint32
; const MAX_THREE_B = 0 :: Uint32
