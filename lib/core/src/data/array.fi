module Data.Array =

import Core
import Core.Ops (Index, index)
import Intrinsics (addr_of, array_index)

impl Index (Array len t) (Int i) t =
    index arr idx =
        array_index arr idx
            |> addr_of
            |> cast
