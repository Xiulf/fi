@intrinsic extern sqrtf32: fn (f: f32) -> f32;
@intrinsic extern sqrtf64: fn (f: f64) -> f64;

--| Not really intrinsic, but required for boxing
@lang "box-alloc"
fn box_alloc(size: usize) -> *()
    libc/malloc(size)
end

--| Not really intrinsic, but required for unboxing
@lang "box-free"
fn box_free(ptr: *())
    libc/free(ptr);
end
