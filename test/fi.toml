[project]
name = "test"
version = "0.1.0"
output = "executable"
# link = ["Ws2_32.lib"]

[dependencies]
prim = { path = "../lib/prim" }
libc = { path = "../lib/libc" }
core = { path = "../lib/core" }
# js = { path = "../lib/js" }
