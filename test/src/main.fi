module Main =

import Prim

@main
main = ()


fib x :: Int32 =
    match x with
    | 0 -> 0
    | 1 -> 1
    | n -> fib (n - 1) + fib (n - 2)


trait Add a =
    add :: a, a -> a

trait Sub a =
    sub :: a, a -> a

infixl 6 (+) = add
infixl 6 (-) = sub

impl Sub Int32 =
    sub = Intrinsics.isub