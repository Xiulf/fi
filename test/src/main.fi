module Main (main) =

import Core
import Data.Slice
import Data.Foldable
import Intrinsics (array_slice)


main () =
    array = [4, 6, 8]
    slice = array_slice array 0 3

    fold slice 0 fn a b -> a + b
