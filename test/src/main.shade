module Main

extern "C" puts: fn (string: *u8) -> i32;

@main
fn main(argc: i32, argv: **u8) -> i32
    print_args(argc, argv);

    0
end

fn print_args(argc: i32, argv: **u8)
    var i = 0;

    while i < argc
        puts(argv.*);
        argv += `*u8`.size;
    end   
end
