module Main =

import Prim
import Intrinsics (unsafe, transmute, ($))


type Option a =
    | None
    | Some a

@main
main =
    let x = None :: Option Int
    
    puts "test"
    ExitCode 0


module C =
    foreign puts :: Ptr Byte -> Int32

puts :: Str -> Int32
puts s =
    let ptr, _ = unsafe (transmute s) :: _, Uint
    C.puts ptr
