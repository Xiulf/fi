module Main where

import Prim (Int, Ptr, Byte, Bool(True, False))

[main]
fn main :: (Int, Ptr (Ptr Byte)) -> Int
fn main argc argv = identity 22

fn identity :: forall t. t -> t
fn identity x = x

const TEST :: Bool
const TEST = True
