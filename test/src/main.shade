module Main =

import Core
import Js
import Js.Console

@main
main =
    let a = 0:1:2:3:4:Nil
    printList a


printList list =
    let go = fn list arr -> case list of
        Cons x xs -> recur xs (push arr x)
        Nil -> log (spread arr)
    
    go list (new "Array")
