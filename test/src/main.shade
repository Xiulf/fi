module Main

import Fmt
import Fmt.Arg as FmtArg

@main
fn main(argc: i32, argv: [*][*:0]u8) -> i32
    var x = Some('a');

    match x
        Some(x): do end
        None: do end
    end

    print_fmt("Hello world!\n", [][..]);

    0
end

enum Option<T>
    Some(val: T)
    None
end
