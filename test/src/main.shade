module Main =

import Prim
import Intrinsics (transmute)

foreign puts :: Ptr Byte -> Int32

type Option a =
    | None
    | Some a

@main
main =
    let x = 0 :: forall a. a where Integer a
    let ptr, _ = transmute "test" :: _, Uint
    puts ptr
    ExitCode 0
