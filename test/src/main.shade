@no_mangle
fn main(argc: i32, argv: **u8) -> i32
    identity(&22).*
end

fn(T) identity(x: *T) -> *T
    x
end

@lang "type-layout"
struct TypeLayout
    size: usize
    align: usize
    stride: usize
end
