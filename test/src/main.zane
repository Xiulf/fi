mod libc
mod util

--| @no_mangle
fn main(argc: i32, argv: ref ref u8) -> i32
    var arr = [2, 4, 8, 16, 32, 64];
    var slice = arr[2..];
    var i = 0;

    while i < slice.len
        util/print_usize(slice[i], 10);
        libc/write(0, ref 10, 1);
        i += 1;
    end

    0
end
