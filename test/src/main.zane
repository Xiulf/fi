main :: fn (argc: #i32, argv: ref ref #u8) -> #i32 {
    i := 0;

    while i < argc {
        c#puts(argv.deref);
        argv = argv + `ref #u8`.size;
        i = i + 1;
    }

    0
};

mod c {
    puts :: extern "C" fn (string: ref #u8) -> #i32;
}
