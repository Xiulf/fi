module Prim =

@lang = "type-kind"
foreign type Type

foreign type Int32

foreign type Array len item
infixr type 1 (*) = Array

@lang = "unit-type"
type Unit = | Unit

trait AnyInt i
trait AnyFloat f

trait Termination t =
    report :: t -> ExitCode

type ExitCode = | ExitCode Int32

impl Termination Unit =
    report () = ExitCode 0