module Prim =

@lang = "type-kind"
foreign type Type

foreign type Int32

foreign type Array len item
infixr type 1 (*) = Array

@lang = "unit-type"
type Unit = | Unit

@lang = "any-int-trait"
trait AnyInt i

@lang = "any-float-trait"
trait AnyFloat f

impl AnyInt Int32

trait Termination t =
    report :: t -> ExitCode

type ExitCode = | ExitCode Int32

impl Termination Unit =
    report () = ExitCode 0