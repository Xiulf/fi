module Prim =

@lang = "kind-kind"
foreign type Kind

@lang = "type-kind"
foreign type Type

@lang = "int-type"
foreign type Int i

@lang = "float-type"
foreign type Float f

foreign type ITag32

type Int32 = Int ITag32

foreign type Array len item
infixr type 1 (*) = Array

@lang = "unit-type"
type Unit = | Unit

@lang = "pair-type"
type Pair a b = | Pair a b

trait Termination t =
    report :: t -> ExitCode

type ExitCode = | ExitCode Int32

impl Termination Unit =
    report () = ExitCode 0