mod vec;

--| @lang "range"
struct Range
    low: usize
    high: usize
end

--| @lang "option"
enum Option(T)
    None
    Some(val: T)
end

--| @lang "type_layout"
struct TypeLayout
    size: usize
    align: usize
    stride: usize
end
